class Dice:
    def roll(self):
        x = random.randint(1,6)
        y = random.randint(1,6)
        print(f"({x}, {y})")


request = input(">").lower()
if "roll" in request:
    rolling = Dice()
    rolling.roll()
